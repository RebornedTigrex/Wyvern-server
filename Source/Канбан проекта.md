---

kanban-plugin: board

---

## Завершено

**Выполнено**
- [x] ### T-001 - Ветка MVP и заморозка Refactor-2
	Создать рабочую ветку от ApprovedMaster и зафиксировать, что Refactor-2 больше не развиваем.
	- ветка mvp-call от ApprovedMaster
	- в AGENTS.md пометка: Refactor-2 заморожена, идеи layout ушли в техдолг
	Готово: ветка создана, git status чист, пометка в AGENTS.md есть.
- [x] ### T-002 - Выкинуть ConnectionV2
	ConnectionV2.cpp и .h - незаконченный копипаст примера сигналинга: вне модульной системы, тянет nlohmann/json, которого нет в зависимостях.
	- удалить оба файла
	Готово: файлов нет, сборка windows-debug-VS2026 зелёная.
- [x] ### T-003 - Удалить P2pMessengerModule целиком
	Полумёртвый мессенджер тащить через T-004, T-007 и T-013 бессмысленно: на T-014 он всё равно переписывается под датаканал. Из 535 строк переезжает меньше трети.
	- перед удалением сохранить хелперы peerIdFromPublicKey и chatIdFromPeerIds в `.agents/loop/status.md` (либо просто помнить, что они остаются в git)
	- удалить P2pMessengerModule.cpp и .h
	- снять регистрацию и инклюд в P2pPlatformModules.cpp, убрать из CmakeLists.txt
	Почему целиком: машина состояний Idle -> Secure, оба обработчика рукопожатия, identity announce и ручное шифрование текста - это работа DTLS, они не переезжают. Плюс activeChatId_ пишется только в handleIdentityAnnounce, так что history без неё всё равно мёртв.
	Готово: собирается, ctest зелёный, приложение стартует, команд p2p.messenger в консоли нет, Core и relay работают.


## В работе

- [ ] ### T-003.5 - Спайк: собрать и проверить libdatachannel
	Страховка перед удалением рабочего транспорта: путь сборки с libdatachannel ещё ни разу не проверялся. В CmakeLists сейчас линкуется LibJuice::LibJuice - это только ICE, rtc::PeerConnection с ним не соберётся.
	- в CmakeLists заменить find_package(LibJuice CONFIG REQUIRED) на find_package(LibDataChannel CONFIG REQUIRED), таргет на LibDataChannel::LibDataChannel
	- временный таргет dc_spike из одного cpp, после задачи удаляется
	- два rtc::PeerConnection в одном процессе: SDP и кандидаты передаются из колбэка одного прямо в метод другого, без сети и без сигналинга
	- включить rtc::InitLogger(rtc::LogLevel::Debug) из global.hpp, иначе ICE отваливается молча
	- датаканал chat, прогнать строку в обе стороны
	Готово: оба PeerConnection дошли до Connected, строка доехала. Только после этого берём T-004.
- [ ] ### T-004 - Удалить самописный транспорт и STUN
	Всё это заменяет libdatachannel: ICE вместо hole punching, libjuice вместо своего STUN.
	- удалить P2pConnectionModule.cpp/h, StunProtocol.h, P2pEvents.h, udpTransportModule.h, UdpPacket.h
	- удалить StunProtocolTests.cpp и P2pLoopbackTests.cpp
	- снять регистрацию в P2pPlatformModules.cpp, поправить CmakeLists.txt
	Готово: собирается, ctest зелёный, в графе больше нет P2pConnectionModule.


## Нужно

- [ ] ### T-005 - SQLite: подключение и схема
	Первый шаг ухода от встроенного Python в хранилище.
	- добавить sqlite3 в vcpkg.json и CmakeLists.txt
	- открытие файла БД по пути из конфига, создание таблиц peers, chats, messages
	Готово: приложение стартует, файл БД создаётся, таблицы на месте.
- [ ] ### T-006 - INodeStore на нативном SQLite
	Заменить Python-реализацию MeshNodeDbModule на нативную по существующему интерфейсу.
	- ensurePeer, getPeer, deletePeer, ensureChat, getChat, deleteChat, saveMessage, loadMessages
	Готово: тест сохранения и чтения сообщения зелёный, история переживает перезапуск.
- [ ] ### T-007 - Удалить встроенный Python и крипто
	DTLS в libdatachannel даёт шифрование канала, отдельный крипто-слой на Python больше не нужен.
	- удалить MeshCryptoModule.cpp/h и PythonRuntimeModule.cpp/h
	- вычистить из CmakeLists BootstrapPythonSDK, BootstrapPythonRuntime, pybind11, Python3::Python
	Готово: собирается без Python, cmake configure заметно быстрее, приложение стартует.
- [ ] ### T-008 - Протокол сигналинга: offer, answer, candidate
	Свести RelayProtocol к тому, что нужно WebRTC.
	- типы сообщений offer, answer, candidate с полями target_overlay_id, sdp, candidate, mid
	- оставить register и rendezvous, лишнее убрать
	Готово: сериализация и разбор покрыты тестом.
- [ ] ### T-009 - RelayServer форвардит сигналинг
	Сервер уже умеет пересылать JSON по id пира - достроить под новые типы.
	- маршрутизация offer, answer, candidate на целевой overlay id
	- внятная ошибка, если пир не подключён
	Готово: два клиента обмениваются offer и answer через сервер.
- [ ] ### T-010 - Конфиг ICE-серверов
	Ради цели "поставить ретранслятор простым изменением конфига".
	- секция iceServers: список url, username, credential
	- дефолты через defaults(), чтение через Core::moduleConfig
	Готово: список читается из конфига и меняется без пересборки.
- [ ] ### T-011 - Скелет DataChannelModule
	Новый и единственный транспорт. Обёртка rtc::PeerConnection в виде модуля.
	- модуль по BaseModule, конфигурация из iceServers
	- команда dc status: состояние соединения и ICE
	Готово: модуль регистрируется, стартует, статус печатается.
- [ ] ### T-012 - Обмен offer, answer и кандидатами
	Связать DataChannelModule с сигналингом.
	- onLocalDescription и onLocalCandidate шлют в сигналинг
	- входящие применяются через setRemoteDescription и addRemoteCandidate
	- события PeerConnectedEvent и PeerDisconnectedEvent в шину
	Готово: два инстанса локально доходят до connected, в логе видны состояния ICE.
- [ ] ### T-013 - Датаканал для текста
	- создание и приём канала chat
	- отправка и приём строк, входящее уходит событием в шину
	Готово: два инстанса локально обмениваются сообщением по датаканалу.
- [ ] ### T-014 - Мессенджер заново поверх датаканала
	Новый модуль вместо удалённого на T-003. Без машины состояний и без ручного крипто - порядка полутора сотен строк.
	- вернуть хелперы peerIdFromPublicKey и chatIdFromPeerIds
	- команды connect <overlayId>, send <текст>, history, status
	- отправка через dc->send(text), входящее пишется в SQLite
	- транспорт заезжает через dependencies() и onInject, как раньше крипто и БД: старый модуль дёргал connection_ мимо DI, это не повторяем
	Готово: КОНТРОЛЬНАЯ ТОЧКА MVP-1. Два инстанса, connect, переписка в обе стороны, история переживает перезапуск.
- [ ] ### T-015 - Проверка через реальный NAT
	- прогон между двумя разными сетями с публичным STUN
	- зафиксировать, какие кандидаты выбрались
	Готово: соединение поднимается, в логе виден srflx-кандидат.
- [ ] ### T-016 - Развернуть coturn
	Готовый TURN вместо своего: своя реализация RFC 5766 съест недели без видимого результата.
	- конфиг coturn, статические креды, открытые порты
	- инструкция по развёртыванию в репозитории
	Готово: turnutils_uclient получает relay-кандидат с сервера.
- [ ] ### T-017 - TURN в конфиге приложения
	- прописать turn-сервер в iceServers
	- режим форса relay-only для проверки
	Готово: КОНТРОЛЬНАЯ ТОЧКА MVP-2. Связь поднимается через relay при симметричном NAT.
- [ ] ### T-018 - Захват и воспроизведение звука
	Рекомендация: miniaudio - один заголовочный файл, без внешних зависимостей.
	- выбор устройства из конфига, захват с микрофона, вывод в динамики
	- команда audio test: локальный луп-бэк
	Готово: запускаешь audio test и слышишь себя.
- [ ] ### T-019 - Кодек Opus
	- opus в vcpkg
	- энкод и декод кадров PCM по 20 мс
	Готово: тест encode -> decode проходит, на слух без артефактов.
- [ ] ### T-020 - Медиа-трек в libdatachannel
	- rtc::Track с Opus, RTP-пакетизация
	- отправка и приём потока между инстансами
	Готово: поток идёт, счётчики отправленных и принятых пакетов растут.
- [ ] ### T-021 - Звонок 1 на 1
	- команды call <overlayId>, answer, hangup
	- состояние звонка и его отображение в статусе
	Готово: КОНЕЧНАЯ ЦЕЛЬ MVP. Два человека в разных сетях слышат друг друга, текст работает до и во время звонка.


## Техдолг

- [ ] ### Layout папок из Refactor-2
	Идея Core/include, Core/sources, Core/submodules сама по себе нормальная. Вернуться после MVP, когда будет что раскладывать. Сейчас перекладывание папок не чинит связность.
- [ ] ### Console IO Api из Refactor-2
	Console.cpp и .h забрать из замороженной ветки, когда текущая консоль реально начнёт мешать. Не раньше.
- [ ] ### Идентичность пиров
	Сверка DTLS-отпечатка через сигналинг, чтобы знать, что на том конце тот, кто заявлен. DTLS шифрует канал, но сам по себе не подтверждает личность.
- [ ] ### Переподключение при обрыве ICE
	Обработка состояний disconnected и failed, повторный сбор кандидатов.
- [ ] ### Судьба InMemoryRelayStore
	После перехода на чистый сигналинг пересмотреть: возможно, хватит карты сессий и очередь сообщений не нужна.
- [ ] ### Групповые звонки
	Mesh на несколько участников. Строго после того, как заработает 1 на 1.
- [ ] ## Изменить модуль интерактивной консоли или Loader
	InteractiveConsoleModule не блокирует поток Loader.cpp - что в свою очередь приводит к моментальной остановке с ошибками (Некоторые модули всё ещё делают работу, а Loader уже вызывает shutdown).
	
	**Нынешнее поведение:** Loader завершается сразу же после того, как запускает io_context().run();
	
	**Ожидаемое поведение:** При чистом запуске Loader с InteractiveConsoleModule - Loader уходит в ожидание, а InteractiveConsoleModule обрабатывает команды и работает корректно


***

## Архивировать

- [x] ### Ядро-фреймворк
	Core (singleton), EventBus, ModuleRegistry, ConfigStore, RuntimeServices, жизненный цикл BaseModule (initialize -> ready -> shutdown), разрешение зависимостей модулей.
- [x] ### Интерактивная консоль
	InteractiveConsoleModule: reader-поток, разбор строки, реестр команд от модулей, help и статусы.
- [x] ### WebSocket relay и сигналинг
	RelayServer на boost-beast и RelaySignalingModule: регистрация overlay id, rendezvous, передача данных, события шины.
- [x] ### Граф проекта
	graphify-out собран из Source: 1095 нод, 43 сообщества. Виден бог-объект P2pConnectionModule и дубль транспорта.
- [x] ### Хранилище relay
	IRelayStore и InMemoryRelayStore: сессии, пиры, очередь сообщений. Покрыто DataStorageTests.

%% kanban:settings
```
{"kanban-plugin":"board","list-collapse":[false,false,false,false]}
```
%%